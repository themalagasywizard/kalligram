import SwiftUI

enum ColorPalette {
    // MARK: - Backgrounds
    static let windowBackground = Color("WindowBackground")
    static let surfacePrimary = Color("SurfacePrimary")
    static let surfaceSecondary = Color("SurfaceSecondary")
    static let surfaceTertiary = Color("SurfaceTertiary")

    // MARK: - Text
    static let textPrimary = Color("TextPrimary")
    static let textSecondary = Color("TextSecondary")
    static let textTertiary = Color("TextTertiary")

    // MARK: - Borders
    static let borderSubtle = Color("BorderSubtle")

    // MARK: - Accents
    static let accentBlue = Color("AccentBlue")
    static let accentAmber = Color("AccentAmber")
    static let aiAccent = Color("AIAccent")
    static let aiAccentLight = Color("AIAccentLight")

    // MARK: - Diff
    static let diffAdded = Color("DiffAdded")
    static let diffAddedBackground = Color("DiffAddedBackground")
    static let diffRemoved = Color("DiffRemoved")
    static let diffRemovedBackground = Color("DiffRemovedBackground")

    // MARK: - Highlights
    static let commentHighlight = Color("CommentHighlight")
    static let citationHighlight = Color("CitationHighlight")

    // MARK: - Editor Special
    static let editorBackground = Color("EditorBackground")
    static let focusBackground = Color("FocusBackground")
    static let readerSepia = Color("ReaderSepia")
}
